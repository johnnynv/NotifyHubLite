apiVersion: v1
kind: Service
metadata:
  name: {{ include "notifyhub-lite.fullname" . }}
  labels:
    {{- include "notifyhub-lite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notifyhub-lite.selectorLabels" . | nindent 4 }}
